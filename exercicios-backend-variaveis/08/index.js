let C;
let A;
let R = 1;

//Calculando o perímetro do círculo
C = 2 * Math.PI * R;

//Calculando a área do círculo

A = Math.PI * R ** 2;

console.log(`O perímetro do círculo é ${C} e a área é ${A}`);
